# NOTE: This file is for native (non-Docker) local dev only — `npm run dev` reads it
# directly. Docker deployments use the root .env + root docker-compose.yml.

# Do not put a / at the end of the URL
VITE_API_URL="http://127.0.0.1:8000"
